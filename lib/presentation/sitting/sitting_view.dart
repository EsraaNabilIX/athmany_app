import 'package:flutter/material.dart';

class SittingView extends StatelessWidget {
  const SittingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

